{
  "name": "bob-plan-client",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:plan && npm run build:map",
    "build:plan": "vite build --mode plan && cp dist/plan.js ../plan.js",
    "build:map": "vite build --mode map && cp dist/map.js ../map.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "@tsconfig/svelte": "^5.0.5",
    "svelte": "^5.43.14",
    "typescript": "^5.9.3",
    "vite": "^7.2.7"
  }
}
